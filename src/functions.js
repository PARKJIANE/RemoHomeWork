const exampleFunction = () => {
    return true;
}

function MathcleFunction(a, b) { 
    const sum = (a, b); {
    const a=1;
    const b=1;
    const sumResult=a+b;
}
const sub = (a, b); {
    const a=1;
    const b=1;
    const subResult=a-b;
}
const div = (a, b); {
    const a=1;
    const b=1;
    const divResult=a/b;
}
const mul = (a, b); {
    const a=1;
    const b=1;
    const mulResult=a*b;
}
return a;
}
// 어디다가 추가하는지
MathcleFunction();
MathcleFunction();
MathcleFunction();
MathcleFunction();


exports.exampleFunction = exampleFunction;

